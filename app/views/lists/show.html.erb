<h1><%= @list.name %></h1>
<ul class=“list-group”>
  <% @list.bookmarks.each do |bookmark| %>
    <li class=“list-group-item”> <strong>Bookmark:</strong> <%= bookmark.comment %></li>
  <% end %>
</ul>
<a href="/lists"> <%= "Back"  %></a>
<%= link_to "Add bookmark", new_list_bookmark_path(@list)%>
